/**
 * @param {string[]} strs
 * @return {string}
 */
var longestCommonPrefix = function(strs) {
  // i have a feeling I would have to use substrings and loops
};

longestCommonPrefix(["flower", "flow", "flight"]);
